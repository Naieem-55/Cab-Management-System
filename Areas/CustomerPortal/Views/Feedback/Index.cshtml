@model PaginatedList<CabManagementSystem.Models.TripFeedback>
@using CabManagementSystem.Models.Enums

@{
    ViewData["Title"] = "My Feedback";
}

<div class="container-fluid">
    <div class="pro-header">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
            <div>
                <h2><i class="bi bi-chat-square-text me-2"></i>My Feedback</h2>
                <p>View your submitted feedback and responses</p>
            </div>
            <span class="pro-stat"><i class="bi bi-chat-dots"></i> @ViewBag.TotalCount Total</span>
        </div>
    </div>

    @if (Model.Any())
    {
        <div class="pro-card">
            <div class="table-responsive">
                <table class="table pro-table mb-0">
                    <thead>
                        <tr>
                            <th>Trip #</th>
                            <th>Route</th>
                            <th>Date</th>
                            <th>Category</th>
                            <th>Rating</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var feedback in Model)
                        {
                            <tr>
                                <td>#@feedback.TripId</td>
                                <td>@feedback.Trip.Route.Origin → @feedback.Trip.Route.Destination</td>
                                <td>@feedback.CreatedDate.ToString("MMM dd, yyyy")</td>
                                <td>
                                    <span class="badge bg-secondary">@feedback.Category</span>
                                </td>
                                <td>
                                    @for (int i = 1; i <= 5; i++)
                                    {
                                        <i class="bi bi-star-fill" style="color: @(i <= feedback.Rating ? "#ffc107" : "#ddd"); font-size: 0.8rem;"></i>
                                    }
                                </td>
                                <td>
                                    @switch (feedback.Status)
                                    {
                                        case FeedbackStatus.Open:
                                            <span class="badge bg-warning text-dark">Open</span>
                                            break;
                                        case FeedbackStatus.InReview:
                                            <span class="badge bg-info">In Review</span>
                                            break;
                                        case FeedbackStatus.Resolved:
                                            <span class="badge bg-success">Resolved</span>
                                            break;
                                        case FeedbackStatus.Closed:
                                            <span class="badge bg-secondary">Closed</span>
                                            break;
                                    }
                                </td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@feedback.Id" class="pro-action">
                                        <i class="bi bi-eye"></i> View
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @if (ViewBag.TotalPages > 1)
            {
                <partial name="_PaginationPartial" />
            }
        </div>
    }
    else
    {
        <div class="pro-card">
            <div class="pro-empty">
                <i class="bi bi-chat-square-text"></i>
                <p>You haven't submitted any feedback yet.</p>
                <a asp-area="CustomerPortal" asp-controller="Trip" asp-action="Index" class="btn btn-primary btn-sm">
                    <i class="bi bi-list-ul me-1"></i> View My Trips
                </a>
            </div>
        </div>
    }
</div>
